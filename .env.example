# URL of the Zibi backend (see server/). All AI queries, auth, wallet
# funding, and order placement go through it — no API keys live in the
# frontend anymore.
VITE_API_BASE_URL=http://localhost:4000
